services:
  postgres:
    image: postgres:16-alpine
    container_name: distil-postgres
    environment:
      POSTGRES_DB: distil
      POSTGRES_USER: distil
      POSTGRES_PASSWORD: distil
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
